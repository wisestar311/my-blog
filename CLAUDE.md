# CLAUDE.md

이 파일은 이 프로젝트에서 작업하는 Claude Code에게 방향을 제공합니다.

## 프로젝트 개요

마크다운(.md) 파일을 읽어서 정적 블로그 웹사이트로 변환하는 프로젝트입니다.
빌드 도구나 프레임워크 없이 순수 HTML, CSS, JavaScript만으로 구현합니다.

## 핵심 요구사항

- **입력**: `posts/` 디렉토리에 있는 마크다운 파일들
- **출력**: 브라우저에서 바로 열리는 정적 블로그 웹사이트
- **디자인**: 깔끔하고 읽기 편한 타이포그래피 중심 레이아웃
- **다크 모드**: 시스템 설정 감지(`prefers-color-scheme`) + 수동 토글, 선택 상태는 `localStorage`에 저장
- **반응형**: 모바일 화면에서도 자연스럽게 보이도록 구현
- **의존성 없음**: npm, 빌드 스텝, 외부 프레임워크(React/Vue 등) 사용 금지. 순수 HTML/CSS/JS만 사용

## 기술 방향

- 마크다운 파싱은 가벼운 자체 구현 또는 단일 파일 라이브러리(예: marked.js를 vendor로 포함) 중 택일 — 외부 CDN 의존 없이 로컬에 파일로 포함
- 마크다운 파일의 frontmatter(제목, 날짜, 태그 등)를 파싱해 포스트 메타데이터로 사용
- 클라이언트 사이드에서 fetch로 `.md` 파일을 읽어 렌더링하거나, 간단한 빌드 스크립트(Node 없이 가능하면 셸/파이썬)로 정적 HTML을 미리 생성하는 방식 중 프로젝트 진행하며 결정
- 별도 서버 없이 정적 파일 서빙만으로 동작해야 함 (GitHub Pages 등에 바로 배포 가능하게)

## 디렉토리 구조 (예상)

```
/
├── index.html          # 블로그 목록 페이지
├── post.html           # 개별 포스트 템플릿
├── css/
│   └── style.css        # 라이트/다크 테마 포함
├── js/
│   ├── main.js
│   └── markdown-parser.js
├── posts/
│   └── *.md              # 블로그 글 원본
└── CLAUDE.md
```

실제 구조는 작업하면서 확정되는 대로 이 섹션을 업데이트합니다.

## 스타일 가이드

- CSS는 커스텀 프로퍼티(CSS 변수)로 라이트/다크 테마 색상 토큰화
- 본문 가독성을 위해 `max-width`로 줄 길이 제한, 여백과 줄간격 넉넉하게
- 모바일 브레이크포인트는 `max-width: 768px` 기준으로 우선 확인
- 불필요한 애니메이션이나 장식 요소 지양, 콘텐츠 중심 디자인

## 작업 시 주의사항

- 새 기능이나 빌드 도구를 추가하기 전에 "프레임워크 없이" 원칙에 맞는지 먼저 확인
- 변경 후에는 브라우저에서 직접 열어 다크 모드 토글, 모바일 뷰(반응형)를 확인
- 이 프로젝트는 아직 git 저장소가 아님 — 커밋 관련 작업 전에 사용자에게 git init 여부 확인
