# 추가 코드 리뷰

이 문서는 기존 `review.md`를 보완하기 위해, 해당 문서가 놓쳤거나 충분히 다루지 않은 보안·상태 관리·모바일 입력·접근성·오류 처리 문제에 초점을 맞춘 추가 리뷰다.

## 처리 결과 요약 (Claude, 리뷰 직후 조치)

| # | 항목 | 처리 |
|---|---|---|
| 1 | Markdown 링크 `javascript:`/`data:` XSS | ✅ 수정 — `js/markdown.js`에 `sanitizeUrl()` 추가, `http(s)/mailto/tel`과 스킴 없는 상대경로만 허용 |
| 2 | 유효하지 않은 날짜가 이스케이프 없이 삽입 | ✅ 수정 — `main.js`/`post.js`에서 `formatDate()` 결과를 `escapeHtml()`로 감싸 출력 |
| 3 | 모바일에 조작 수단 없음 | ✅ 수정 — 스와이프 제스처(`pointerdown/up`) + 온스크린 방향 버튼(`.game-dpad`) 추가, 안내 문구도 갱신 |
| 4 | 승리 오버레이 중 입력이 게임 상태를 변경 | ✅ 수정 — `isOverlayOpen` 상태 추가, 오버레이 표시 중엔 키보드/스와이프/D-패드 입력 모두 차단 |
| 5 | `localStorage` 예외로 초기화가 중단됨 | ✅ 수정 — `theme.js`/`game.js`의 스토리지 read/write를 `try/catch`로 감싸 실패 시 메모리 기본값으로 계속 동작 |
| 6 | 픽셀 아트 캔버스에 `touch-action` 없음 | ✅ 수정 — `#pixel-canvas`에 `touch-action: none` 추가 |
| 7 | 빠른 드래그 시 셀 보간 없음 | ✅ 수정 — Bresenham 선 알고리즘으로 `pointermove` 사이 셀을 채우는 `paintLine()` 추가 |
| 8 | 픽셀 아트가 키보드/스크린 리더에 노출 안 됨 | ⚠️ 부분 수정 — 팔레트 스와치에 색상명 `aria-label` + `aria-pressed` 추가. 캔버스 자체의 키보드 대체 조작(셀 커서 이동)은 별도의 UI 설계가 필요한 규모라 이번엔 보류 |
| 9 | 2048 상태 변화가 보조기술에 전달 안 됨 | ⚠️ 부분 수정 — 점수에 `aria-live`, 오버레이에 `role="alertdialog"`/`aria-modal`/포커스 이동 추가. 보드 전체를 텍스트로 설명하는 완전한 대안 UI는 보류 |
| 10 | 포스트 fetch 오류 미검사 + 하나 실패 시 전체 목록 실패 | ✅ 수정 — `res.ok` 검사 추가, `Promise.all` → `Promise.allSettled`로 교체해 개별 실패만 건너뜀 |
| 11 | 시스템 테마 변경이 캔버스/토글에 반영 안 됨 | ✅ 수정 — `theme.js`가 `matchMedia` change와 토글 클릭 모두에서 `blogthemechange` 커스텀 이벤트를 발생시키고, `pixel-art.js`가 이를 구독하도록 변경(기존 `MutationObserver` 방식 대체) |
| 12 | 목록 페이지가 매번 전체 포스트 본문을 다운로드 | ⏭️ 보류 — `posts.json`에 메타데이터를 함께 생성하는 빌드 단계가 필요한 아키텍처 변경이라 이번 범위에서는 제외 |
| 13 | PNG 인코딩 실패 시 `null` Blob 미처리 | ✅ 수정 — `exportPNG()`에 `if (!blob)` 가드 + 안내 alert 추가 |
| 14 | 날짜 전용 문자열을 UTC로 파싱해 시간대에 따라 하루 어긋남 | ✅ 수정 — `formatDate()`가 `YYYY-MM-DD`를 로컬 날짜로 직접 생성하도록 변경 |
| 15 | 테마 토글 `aria-label`이 상태와 무관하게 고정 | ✅ 수정 — `updateToggleLabel()`에서 다음 동작에 맞춰 `aria-label`도 함께 갱신 |

12번(성능)과 8·9번의 나머지 부분은 별도 논의 후 진행하는 게 맞다고 판단해 이번 커밋에는 포함하지 않았다.

1. **Markdown 링크의 위험한 URL 스킴으로 DOM XSS가 가능함**
   - **파일/위치:** `js/markdown.js:36-48`의 `renderInline()`, 특히 `out.replace(..., '<a href="$2">$1</a>')`; `js/post.js:43`
   - **문제 설명:** 링크 URL을 HTML 이스케이프하기는 하지만 `javascript:`, `data:` 같은 실행 가능 스킴을 차단하지 않은 채 `href`에 넣고, 결과를 `innerHTML`로 삽입한다. 따라서 공격자가 포스트 Markdown을 수정할 수 있으면 독자가 링크를 클릭할 때 동일 출처 페이지에서 스크립트를 실행할 수 있다. 이스케이프는 속성 탈출을 막을 뿐 위험한 URL 자체를 안전하게 만들지는 않는다.
   - **재현 시나리오 또는 근거:** 본문에 ``[클릭](javascript:document.body.textContent='pwned')``를 넣으면 `renderMarkdown()` 결과가 `<a href="javascript:document.body.textContent=&#39;pwned&#39;">클릭</a>`가 된다. `post.js:43`이 이를 `.post-content` 안에 `innerHTML`로 넣으므로 링크 클릭 시 JavaScript URL이 실행된다. 현재 HTML에는 이를 방어할 CSP도 없다.
   - **심각도:** High
   - **제안하는 수정 방향:** URL을 `new URL()` 등으로 파싱해 `http:`, `https:` 및 필요한 상대 URL만 허용하고 그 외 스킴은 제거한다. 가능하면 검증된 Markdown 라이브러리와 sanitizer(DOMPurify 등)를 함께 사용하고 CSP도 보조 방어선으로 추가한다.

2. **유효하지 않은 frontmatter 날짜가 이스케이프 없이 삽입되어 저장형 XSS가 가능함**
   - **파일/위치:** `js/main.js:4-8, 25`, `js/post.js:4-8, 40`
   - **문제 설명:** `formatDate()`는 파싱할 수 없는 날짜를 원문 그대로 반환한다. 호출부는 그 반환값을 이스케이프하지 않고 `innerHTML` 템플릿에 삽입하므로, `date` 메타데이터가 HTML이면 목록과 상세 페이지 모두에서 실행된다. 제목·요약·태그에는 적용한 `escapeHtml()`이 날짜에만 빠져 있다.
   - **재현 시나리오 또는 근거:** frontmatter에 `date: <img src=x onerror=alert(document.domain)>`를 두면 `new Date(dateStr)`는 Invalid Date이고 `if (isNaN(d)) return dateStr;` 분기로 원문이 반환된다. 이후 `main.js:25`의 `<div class="post-item-meta">${formatDate(meta.date)}</div>` 또는 `post.js:40`이 해당 태그를 실제 DOM으로 파싱한다.
   - **심각도:** High
   - **제안하는 수정 방향:** `formatDate()`의 모든 반환값을 텍스트로 취급해 호출부에서 `escapeHtml()`을 적용하거나, 날짜 노드를 생성해 `textContent`로 넣는다. frontmatter 단계에서도 허용된 날짜 형식(예: `YYYY-MM-DD`)만 받도록 검증한다.

3. **모바일에서 2048의 핵심 조작 수단이 없음**
   - **파일/위치:** `game.html:42`, `js/game.js:233-245`, `css/game.css:218-240`
   - **문제 설명:** UI는 “방향키”만 안내하고 실제 입력도 전역 `keydown`의 네 방향키만 처리한다. 모바일 레이아웃용 미디어 쿼리는 있지만 터치/스와이프나 화면 방향 버튼이 없어, 물리 키보드가 없는 일반적인 스마트폰에서는 게임을 진행할 수 없다.
   - **재현 시나리오 또는 근거:** 터치 기기에서 `game.html`을 열면 New Game 버튼은 누를 수 있지만 보드 이동용 컨트롤은 생성되지 않는다. `game.js` 전체에서 이동을 호출하는 사용자 입력 리스너는 `document.addEventListener("keydown", ...)` 하나뿐이며 `pointer*`, `touch*`, `click` 기반 이동 처리가 없다.
   - **심각도:** High
   - **제안하는 수정 방향:** 보드에 스와이프 제스처를 추가하고, 키보드와 보조기술 사용자도 쓸 수 있는 네 방향 버튼을 함께 제공한다. 제스처에는 최소 이동 거리와 방향 판정을 두고 페이지 스크롤과 충돌하지 않게 처리한다.

4. **승리 오버레이가 열린 동안에도 키보드 입력이 뒤의 게임 상태를 변경함**
   - **파일/위치:** `js/game.js:139-155`의 `checkWin()`, `js/game.js:183-195`의 오버레이 처리, `js/game.js:233-245`의 키 입력 처리
   - **문제 설명:** 2048 타일 생성 시 `hasWon`만 `true`가 되고 `isGameOver`는 여전히 `false`다. 오버레이는 시각적으로만 표시되며, 키 핸들러는 `isGameOver`만 검사하므로 사용자가 “Try Again” 또는 “Keep Playing”을 고르기 전에도 방향키로 보드를 계속 움직일 수 있다.
   - **재현 시나리오 또는 근거:** `checkWin()`은 `showOverlay("You Win!", true)` 후 반환하지만 입력 잠금 상태를 설정하지 않는다. 이어지는 키 이벤트는 `if (isGameOver) return; move(direction);`를 통과한다. 그 결과 오버레이 뒤에서 타일이 합쳐지고 새 타일도 생성되며, 나중에 Keep Playing을 누르면 사용자가 보지 못한 변경 상태에서 재개된다.
   - **심각도:** Medium
   - **제안하는 수정 방향:** `isOverlayOpen`/게임 단계 상태를 명시적으로 두고 선택 전 이동을 차단한다. 오버레이를 `role="dialog"`, `aria-modal="true"`로 만들고 표시 시 첫 버튼으로 포커스를 이동하며, 닫을 때 적절한 위치로 복귀시킨다.

5. **`localStorage` 접근 실패가 테마 스크립트와 2048 초기화를 통째로 중단시킴**
   - **파일/위치:** `js/theme.js:5-7, 23, 36`, `js/game.js:176-180, 261-265`; 모든 HTML의 head에서 로드하는 `js/theme.js`
   - **문제 설명:** 저장소 읽기/쓰기를 모두 예외 처리 없이 수행한다. 저장소가 비활성화되었거나 브라우저 정책·샌드박스·일부 `file:` 환경에서 `SecurityError`가 발생하면, `theme.js`는 `window.BlogTheme`를 만들기 전인 23행에서 종료될 수 있다. 2048도 DOMContentLoaded 안의 점수 읽기에서 예외가 나면 `initGame()`까지 도달하지 못한다.
   - **재현 시나리오 또는 근거:** `localStorage.getItem()`이 던지는 환경에서는 `applyTheme(getStoredTheme())`가 실패하고, 각 HTML 마지막의 `BlogTheme.initThemeToggle(...)`도 `BlogTheme is not defined`를 낸다. 게임에서는 `const storedBest = localStorage.getItem(...)` 다음 줄의 `initGame()`이 실행되지 않아 보드가 빈 상태로 남는다.
   - **심각도:** Medium
   - **제안하는 수정 방향:** 저장소 접근을 작은 `try/catch` 래퍼로 감싸고 실패 시 메모리 기본값으로 계속 동작시킨다. 점수 저장 실패는 게임 진행을 막지 않게 하고, 테마는 시스템 설정 또는 라이트 기본값으로 폴백한다.

6. **픽셀 아트 캔버스가 브라우저의 터치 스크롤 제스처를 비활성화하지 않음**
   - **파일/위치:** `js/pixel-art.js:93-115`, `css/pixel-art.css:45-52`
   - **문제 설명:** 캔버스는 Pointer Events로 드래그를 구현했지만 `touch-action: none`이 없고 이벤트에서 기본 동작도 제어하지 않는다. 포인터 캡처는 브라우저의 스크롤/확대 제스처를 금지하지 않으므로, 터치 드래그가 페이지 스크롤로 판정되면 `pointercancel`이 발생해 선이 중간에서 끊길 수 있다.
   - **재현 시나리오 또는 근거:** `#pixel-canvas` CSS에는 크기·렌더링·테두리만 있고 `touch-action` 선언이 없다. JS는 `pointercancel` 시 단순히 `isPointerDown = false`로 끝내므로, 모바일에서 캔버스를 위아래로 길게 칠할 때 브라우저가 스크롤을 시작하는 순간 그리기가 종료된다.
   - **심각도:** Medium
   - **제안하는 수정 방향:** 그리기 표면에 `touch-action: none`을 선언한다. 필요하면 페이지 전체가 아니라 캔버스에서만 기본 제스처를 제한하고, `pointercancel` 이후 상태도 확실히 정리한다.

7. **빠른 드래그에서 포인터 이벤트 사이의 셀을 보간하지 않아 빈 칸이 생길 수 있음 (조건부)**
   - **파일/위치:** `js/pixel-art.js:83-90`의 `paintAtEvent()`, `js/pixel-art.js:100-108`의 `pointermove` 처리
   - **문제 설명:** 각 `pointermove`가 가리키는 단일 셀만 칠하고 직전 좌표와 현재 좌표 사이를 보간하지 않는다. 기존 `review.md`는 성긴 자동화 드래그를 도구 문제로 분류했지만, 실제 브라우저도 이동 속도·기기 이벤트 주기·부하에 따라 한 셀 폭보다 먼 간격으로 이벤트를 전달할 수 있으므로 같은 현상이 실사용에서도 발생할 수 있다.
   - **재현 시나리오 또는 근거:** 한 이벤트가 `(row 0, col 0)`, 다음 이벤트가 `(row 0, col 4)`에서 발생하면 현재 코드는 0번과 4번 셀에만 `paintCell()`을 호출하고 1~3번 셀은 건드리지 않는다. 특히 CSS로 축소된 캔버스나 빠른 펜/마우스 이동에서 재현 가능성이 커진다. 다만 발생 빈도는 입력 장치와 속도에 의존한다.
   - **심각도:** Medium
   - **제안하는 수정 방향:** 마지막으로 칠한 셀을 저장하고 Bresenham 선 알고리즘 등으로 두 셀 사이를 채운다. 지원 브라우저에서는 `getCoalescedEvents()`의 중간 좌표도 함께 처리한다.

8. **픽셀 아트 편집의 핵심 기능이 키보드 및 스크린 리더에 노출되지 않음**
   - **파일/위치:** `pixel-art.html:31-40`, `js/pixel-art.js:93-115, 124-157`
   - **문제 설명:** 캔버스에 접근 가능한 이름·역할·포커스 진입점·키보드 조작이 없고 그리기는 포인터 이벤트만 받는다. 또한 20개 색상 버튼이 모두 똑같은 `aria-label="색상 선택"`을 가지며, 현재 선택 상태는 `.selected` CSS 클래스에만 있고 `aria-pressed` 같은 상태 정보가 없다.
   - **재현 시나리오 또는 근거:** 키보드로 팔레트 버튼과 Clear/Save에는 접근할 수 있지만 캔버스 셀로 이동하거나 칠할 방법이 없다. 스크린 리더의 버튼 목록에는 서로 구분되지 않는 “색상 선택” 버튼이 반복되고 어떤 색이 선택됐는지도 전달되지 않는다.
   - **심각도:** Medium
   - **제안하는 수정 방향:** 캔버스에 명확한 레이블과 키보드 기반 셀 커서/칠하기 조작을 제공하거나, 접근 가능한 16×16 그리드 대체 UI를 제공한다. 각 스와치 이름에 색상명을 포함하고 `aria-pressed`를 선택 변경 때 동기화한다.

9. **2048의 보드·점수·결과 변화가 보조기술에 전달되지 않음**
   - **파일/위치:** `game.html:29-37, 46-69`, `js/game.js:197-219`
   - **문제 설명:** 점수는 일반 `span`, 보드와 타일은 의미 없는 `div`로만 구성되고 결과 오버레이에도 `role`, 라이브 영역, 포커스 관리가 없다. 키보드로 이동 자체는 가능해도 스크린 리더 사용자는 현재 타일 배치, 점수 변화, 승리/게임 오버 발생을 알기 어렵다.
   - **재현 시나리오 또는 근거:** `render()`는 매 이동마다 `.tile` div를 제거·재생성하고 숫자를 `textContent`로 넣지만, `#game-board`와 `#score-value` 어느 곳에도 `aria-label`/`aria-live`가 없다. `showOverlay()`도 `hidden` 속성만 제거할 뿐 결과 메시지를 공지하거나 버튼으로 포커스를 옮기지 않는다.
   - **심각도:** Medium
   - **제안하는 수정 방향:** 점수와 결과에 적절한 라이브 영역을 두고, 보드 상태를 간결한 텍스트로 설명하는 접근성 전용 상태를 갱신한다. 오버레이는 실제 대화상자 패턴과 포커스 관리를 적용한다.

10. **목록에서 개별 포스트 HTTP 오류를 검사하지 않아 유령 항목이 생기며, 한 번의 네트워크 실패가 전체 목록을 지움**
    - **파일/위치:** `js/main.js:39-46, 55-58`
    - **문제 설명:** manifest 응답에는 `ok` 검사가 있지만 각 Markdown 응답에는 없다. HTTP 404/500도 `res.text()`로 정상 처리되어 오류 페이지 본문을 포스트처럼 파싱하고 파일명을 제목으로 한 항목을 만든다. 반대로 단 하나의 실제 fetch 거부가 발생하면 `Promise.all()` 전체가 실패해 정상적으로 받은 다른 글까지 모두 숨긴다.
    - **재현 시나리오 또는 근거:** `posts/posts.json`에 존재하지 않는 `missing.md`를 넣으면 41~44행은 404 응답을 거부하지 않고 `{ filename: "missing.md", meta: {} }`를 반환한다. 네트워크 계층 오류를 하나 발생시키면 39행의 `Promise.all()`이 reject되어 57행의 전체 목록 오류 메시지만 표시된다.
    - **심각도:** Medium
    - **제안하는 수정 방향:** 각 응답의 `res.ok`를 확인하고 실패 파일을 명시적으로 처리한다. 일부 글 실패가 전체 목록을 막지 않도록 `Promise.allSettled()` 또는 개별 `try/catch`를 사용하고, 실패 항목은 제외하면서 진단 가능한 로그/메시지를 남긴다.

11. **실행 중 시스템 테마 변경은 픽셀 캔버스와 토글 아이콘에 반영되지 않음**
    - **파일/위치:** `css/style.css:41-58`, `js/theme.js:17-28, 30-39`, `js/pixel-art.js:204-209`
    - **문제 설명:** 저장된 테마가 없을 때 CSS는 `prefers-color-scheme` 변경에 즉시 반응하지만, JS는 해당 미디어 쿼리의 `change` 이벤트를 구독하지 않는다. 픽셀 아트의 기존 보완 코드는 `data-theme` 속성 변화만 관찰하므로 OS 설정이 바뀌어도 속성이 그대로인 경우 캔버스는 재렌더되지 않고 토글 이모지도 이전 상태로 남는다.
    - **재현 시나리오 또는 근거:** `localStorage.theme`과 `data-theme`이 없는 상태에서 페이지를 연 뒤 OS 테마를 라이트에서 다크로 바꾸면 CSS 변수는 미디어 쿼리로 바뀐다. 그러나 `MutationObserver`의 `attributeFilter: ["data-theme"]` 조건은 발생하지 않고, `updateToggleLabel()`도 초기화와 버튼 클릭 때만 호출된다.
    - **심각도:** Medium
    - **제안하는 수정 방향:** `matchMedia("(prefers-color-scheme: dark)")`의 `change`를 구독해 저장된 명시적 선택이 없을 때 레이블과 캔버스를 갱신한다. 공용 테마 모듈이 사용자 정의 이벤트를 발생시키고 각 페이지 기능이 이를 구독하게 하면 결합도를 줄일 수 있다.

12. **목록 페이지가 메타데이터를 위해 모든 Markdown 본문을 매번 다운로드함**
    - **파일/위치:** `js/main.js:35-46`
    - **문제 설명:** `posts.json`에는 파일명만 들어온다는 전제 아래, 홈 화면은 제목·날짜·요약·태그만 필요하면서도 모든 포스트 원문을 병렬로 내려받고 frontmatter만 추출한다. 글 수와 본문/이미지 참조 전 텍스트 크기가 늘수록 초기 목록 표시의 전송량과 요청 수가 선형으로 증가한다.
    - **재현 시나리오 또는 근거:** 목록 로드마다 manifest 1회 후 `filenames.map()`의 모든 파일에 `fetch()`를 실행한다. 포스트가 100개면 홈 진입만으로 추가 요청 100개가 발생하고 가장 늦은 요청이 끝날 때까지 `Promise.all()` 때문에 목록이 렌더되지 않는다.
    - **심각도:** Low
    - **제안하는 수정 방향:** 빌드 시 `posts.json`에 목록에 필요한 메타데이터를 함께 생성한다. 페이지네이션/지연 로딩 또는 제한된 동시성도 글 수가 많은 경우의 대안이다.

13. **PNG 인코딩 실패 시 `null` Blob을 처리하지 않아 저장 버튼이 예외로 끝남**
    - **파일/위치:** `js/pixel-art.js:185-194`의 `exportPNG()`
    - **문제 설명:** `HTMLCanvasElement.toBlob()` 콜백의 값은 인코딩 실패 시 `null`일 수 있지만 곧바로 `URL.createObjectURL(blob)`에 전달한다. 실패 시 사용자 안내 없이 `TypeError`가 발생한다. 캔버스 컨텍스트 생성 실패도 별도로 검사하지 않는다.
    - **재현 시나리오 또는 근거:** 185행 콜백은 `blob`의 존재 여부를 검사하지 않으며 186행이 즉시 객체 URL을 만든다. 사양상 nullable인 결과가 `null`이면 이 호출은 유효한 `Blob`/`MediaSource`가 아니므로 실패한다.
    - **심각도:** Low
    - **제안하는 수정 방향:** `if (!blob)` 분기에서 오류 메시지를 표시하고 다운로드를 중단한다. `getContext("2d")` 결과도 초기화 시 확인해 지원되지 않는 환경에서 기능을 비활성화한다.

14. **날짜 전용 문자열을 UTC 시각으로 파싱해 일부 시간대에서 하루 전 날짜로 표시함**
    - **파일/위치:** `js/main.js:4-8`, `js/post.js:4-8`
    - **문제 설명:** `YYYY-MM-DD`를 `new Date(dateStr)`로 파싱하면 UTC 자정으로 해석된다. UTC보다 느린 시간대의 독자는 로컬 변환 과정에서 전날 날짜를 보게 되어 발행일이 사용자 위치에 따라 달라진다.
    - **재현 시나리오 또는 근거:** 예를 들어 미국 서부 시간대에서 `new Date("2026-08-22").toLocaleDateString("ko-KR", ...)`는 UTC 2026-08-22 00:00이 현지 2026-08-21이므로 `2026년 8월 21일`로 표시될 수 있다. 두 페이지가 같은 구현을 중복 사용한다.
    - **심각도:** Low
    - **제안하는 수정 방향:** 날짜-only 값은 연·월·일을 직접 분리해 포맷하거나 `timeZone: "UTC"`를 지정한다. 사이트 발행 기준 시간대를 명시적으로 선택해 공용 함수 하나로 사용한다.

15. **테마 토글의 접근 가능한 이름이 현재 상태와 무관하게 항상 “다크 모드 전환”임**
    - **파일/위치:** `index.html:18`, `post.html:18`, `game.html:19`, `pixel-art.html:19`; `js/theme.js:25-28`
    - **문제 설명:** 버튼의 보이는 이모지는 현재 테마에 맞춰 바뀌지만 고정 `aria-label`이 접근 가능한 이름을 덮어쓴다. 이미 다크 모드인 상태에서도 스크린 리더는 “다크 모드 전환”이라고 읽어 실제 클릭 결과인 라이트 모드 전환과 반대로 안내한다.
    - **재현 시나리오 또는 근거:** 네 페이지 모두 동일한 `aria-label="다크 모드 전환"`을 사용하고, `updateToggleLabel()`은 `textContent`만 변경할 뿐 속성을 갱신하지 않는다.
    - **심각도:** Low
    - **제안하는 수정 방향:** `updateToggleLabel()`에서 다음 동작에 맞춰 `aria-label`도 “라이트 모드로 전환”/“다크 모드로 전환”으로 동기화하고, 필요하면 `aria-pressed`로 현재 상태를 별도로 전달한다.
